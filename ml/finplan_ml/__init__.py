"""FinPlan ML — segmentation, allocation, forecasting, RAG and planning agents.

Importable as a package so the FastAPI backend can serve these models directly
rather than shelling out to a notebook or a second process.
"""

__all__ = ["config"]
