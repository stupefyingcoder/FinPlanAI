# Set these in Vercel -> Project -> Settings -> Environment Variables.
# Create React App inlines them at BUILD time, so changing one needs a redeploy.

# The Render API URL, no trailing slash.
REACT_APP_API_URL=https://finplan-api.onrender.com
