# FinPlan AI backend

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic[email]>=2.6
python-dotenv>=1.0

# Persistence
SQLAlchemy>=2.0
alembic>=1.13
PyMySQL>=1.1          # MySQL in production; SQLite needs no driver

# Auth
PyJWT>=2.8
bcrypt>=4.1

# Tests
pytest>=8.0
httpx>=0.27

# The ML package is imported from ../ml (see app/main.py). Its own
# dependencies are pinned in ml/requirements.txt.
